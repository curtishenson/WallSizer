WallSizer is an HTML5 Canvas experiment. It resizes any image to the most popular monitor wallpaper sizes using only Canvas and HTML5. Simply drag an image into the drop zone the save the thumbnail. Thumbnails will save as a full size image.